使用方法:
excel2sqlite C:\\目录 C:\\数据库.db 导入表名 excel中sheet的名字 字段1,字段2,字段3  
  
参数1：传入目录名字，程序可以扫描改目录(不包含子目录)中所有.xls和.xlsx后缀名的excel文件  
参数2：要保存到sqlite的数据库的文件名  
参数3：要导入的表的表名，这个表要提前create好，字段也要创建好，程序不会自动去创建表和列名。  
参数4：要读取excel的工作表的表名  
参数5: 字段以逗号分隔，然后程序会自动到excel的第一行是标题栏查找相应列，自动匹配excel表中的列和数据库中的列  